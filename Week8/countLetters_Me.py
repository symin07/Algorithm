def countLetters(word):
    counter = {}    # inttialize set
                    # set is collectio data type: unodered, iterative, variable, unique: duplicate elements are not
                    # A set itself may be modifeid, but the element contained in the set must be of an immutable type